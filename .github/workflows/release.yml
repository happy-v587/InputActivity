name: Release Builds

on:
  release:
    types:
      - published
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macos-arm64
            os: macos-latest
            platform: mac
            arch: arm64
            targets: dmg zip
          - name: macos-x64
            os: macos-15-intel
            platform: mac
            arch: x64
            targets: dmg zip
          - name: windows-x64
            os: windows-latest
            platform: win
            arch: x64
            targets: nsis zip
          - name: linux-x64
            os: ubuntu-latest
            platform: linux
            arch: x64
            targets: AppImage deb tar.gz

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libx11-dev libxkbfile-dev libxkbcommon-dev libxt-dev libxtst-dev libxrandr-dev

      - name: Install dependencies
        run: npm ci

      - name: Sync package version from release tag
        if: github.event_name == 'release' || (github.event_name == 'push' && github.ref_type == 'tag')
        env:
          RELEASE_TAG: ${{ github.event_name == 'release' && github.event.release.tag_name || github.ref_name }}
        shell: bash
        run: |
          version="${RELEASE_TAG#v}"
          npm version --no-git-tag-version "$version"

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Lint
        run: npm run lint

      - name: Build renderer and main process
        run: npm run build

      - name: Rebuild native modules for Electron
        run: npm run rebuild:native:electron

      - name: Package desktop app
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
        shell: bash
        run: |
          npx electron-builder --${{ matrix.platform }} ${{ matrix.targets }} --${{ matrix.arch }} --publish never

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: input-activity-${{ matrix.name }}
          if-no-files-found: error
          path: |
            release/*.AppImage
            release/*.deb
            release/*.dmg
            release/*.exe
            release/*.tar.gz
            release/*.zip
            release/*.yml
            release/*.blockmap

      - name: Upload files to GitHub Release
        if: github.event_name == 'release' || (github.event_name == 'push' && github.ref_type == 'tag')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.event_name == 'release' && github.event.release.tag_name || github.ref_name }}
        shell: bash
        run: |
          shopt -s nullglob
          files=(
            release/*.AppImage
            release/*.deb
            release/*.dmg
            release/*.exe
            release/*.tar.gz
            release/*.zip
            release/*.yml
            release/*.blockmap
          )
          if [ "${#files[@]}" -eq 0 ]; then
            echo "No release assets found."
            exit 1
          fi
          if ! gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release create "$RELEASE_TAG" \
              --target "$GITHUB_SHA" \
              --title "$RELEASE_TAG" \
              --notes "Automated release for $RELEASE_TAG." \
              || gh release view "$RELEASE_TAG" >/dev/null
          fi
          gh release upload "$RELEASE_TAG" "${files[@]}" --clobber
